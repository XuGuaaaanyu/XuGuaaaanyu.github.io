Place your resume source at `resume/main.tex`.

Manual workflow:
- Compile locally to generate `resume/main.pdf`.
- Commit both files together:
  - `resume/main.tex`
  - `resume/main.pdf`
- Push once.

Site behavior:
- The `CV` tab points to `/resume/main.pdf`.
- No GitHub Action is used for resume PDF generation.
